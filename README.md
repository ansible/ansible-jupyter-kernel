# ansible_kernel

## To install:

    pip install -e .
    python -m ansible_kernel.install

## To use it:

```
    jupyter notebook
    # In the notebook interface, select Ansible from the 'New' menu
```
